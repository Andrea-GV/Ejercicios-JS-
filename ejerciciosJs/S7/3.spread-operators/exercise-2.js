// Dado el siguiente objeto, crea una copia usando spread operators.

const toy = {name: 'Bus laiyiar', date: '20-30-1995', color: 'multicolor'};
console.log(toy);

const toyClonado = { ...toy };
console.log(toyClonado);